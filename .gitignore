# --- данные ---
data/raw/
*.geojson
*.geojson.zip

# Сгенерированные датасеты сайта: пересобираются weekly и живут в GitHub Release
# asset data-latest, а не в git-истории. Уже закоммиченные файлы остаются (историю
# не переписываем), но новые пересборки больше не попадают в main.
web/public/data/

# --- node ---
node_modules/
web/dist/
*.local

# --- логи и мусор ---
*.log
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# --- IDE ---
.idea/
.vscode/

# --- временные токены ---
gh_dtp_token.txt

# --- секреты ---
.env
.env.*
cloudfire.md
*.key
*credentials*
