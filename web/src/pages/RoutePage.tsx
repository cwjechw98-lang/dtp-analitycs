import RouteTab from "../components/RouteTab";

/**
 * Раздел «Маршрут» — точка входа по умолчанию.
 *
 * Этап 1: контейнер вокруг существующего RouteTab, внутренности не тронуты.
 * Этап 2: сюда переедет слой правил из tips.json, отфильтрованный по
 * контексту конкретного маршрута, и блок благодарности после отчёта.
 */
export default function RoutePage() {
  return <RouteTab />;
}
